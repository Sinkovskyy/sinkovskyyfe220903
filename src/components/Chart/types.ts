export type TData = TChartValue[];

export type TChartValue = {
  name: string;
  time: number;
};

export type TGetPositionInPercentage = {
  index: number;
};

export type TGetWidthInPercentage = {
  index: number;
  time: string;
};
